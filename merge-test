merge-test

revert 是因为上面这句话在”不正确的时机“被合入了master，现在我又加了一些改动，并且”时机正确（例如该我组上beta了）“，那么我需要将这次的改动 + 上面这句话（之前的改动）一起merge到master



--- 

上面增加了第2句话之后，可以把第1句话（revert）一起merge进master，我理解”这是同一个文件，如果是不同的文件，并且已经没有新的改动了（revert 了很多文件，不可能为了能被merge进去，找到每个文件，并还在每个文件添加一些新的改动）“，所以这次测试下。这句话到时候会merge到master，再被revert掉；同时还有另一个文件，另一个文件跟随这个文件的操作，不同的是另外这个文件会在再次merge之前添加一些改动（相当于上述两句话的操作）